# CNB 流水线配置 - 通用文件下载任务
# 用途：下载用户有权限的文件并处理

"**":
  api_trigger_download:
    - name: file-download-task
      docker:
        image: node:20-slim
      stages:
        - name: Install deps
          script: |
            set -euo pipefail
            echo "📦 安装依赖..."
            sed -i 's/deb.debian.org/mirrors.cloud.tencent.com/g' /etc/apt/sources.list.d/debian.sources 2>/dev/null || true
            apt-get update
            apt-get install -y --no-install-recommends ca-certificates curl aria2
            update-ca-certificates
            rm -rf /var/lib/apt/lists/*
            echo "✅ 依赖安装完成"

        - name: System info
          script: |
            echo "=== 系统信息 ==="
            uname -a || true
            echo ""
            echo "=== 磁盘空间 ==="
            df -h / || true
            echo ""
            echo "=== 内存 ==="
            free -h || true
            echo ""
            echo "=== 网络 ==="
            ip addr 2>/dev/null | grep -E "inet " || echo "无法获取 IP"
            echo ""
            echo "=== 外网 IP ==="
            curl -fsSL --connect-timeout 5 ifconfig.me 2>/dev/null || echo "无法获取"
            echo ""
            echo "================"

        - name: Validate env
          script: |
            set -euo pipefail
            echo "� 验证环境变量..."
            
            # 检查必需变量（不输出具体值）
            : "${DOWNLOAD_URL:?DOWNLOAD_URL is required}"
            echo "  DOWNLOAD_URL: ${#DOWNLOAD_URL} chars ✅"
            
            # 可选变量检查
            if [ -n "${TARGET_FOLDER:-}" ]; then
              echo "  TARGET_FOLDER: ${TARGET_FOLDER} ✅"
            else
              echo "  TARGET_FOLDER: (default: downloads)"
            fi
            
            if [ -n "${TIMEOUT_MINUTES:-}" ]; then
              echo "  TIMEOUT_MINUTES: ${TIMEOUT_MINUTES} ✅"
            else
              echo "  TIMEOUT_MINUTES: (default: 30)"
            fi
            
            echo "✅ 环境变量验证通过"

        - name: Prepare workspace
          script: |
            set -euo pipefail
            echo "� 准备工作目录..."
            mkdir -p /downloads
            mkdir -p /root/.cache/aria2
            echo "✅ 目录创建完成"

        - name: Download file
          script: |
            set -euo pipefail
            echo "=========================================="
            echo "🚀 开始下载"
            echo "=========================================="
            
            # 设置超时（默认30分钟）
            TIMEOUT_MINUTES="${TIMEOUT_MINUTES:-30}"
            TIMEOUT_SECONDS=$((TIMEOUT_MINUTES * 60))
            echo "⏱️ 超时设置: ${TIMEOUT_MINUTES} 分钟"
            echo ""
            
            # 开始下载
            echo "⏳ 启动下载..."
            START_TIME=$(date +%s)
            
            # 使用 timeout 命令控制最大执行时间
            timeout "${TIMEOUT_SECONDS}s" aria2c "$DOWNLOAD_URL" \
              --dir=/downloads \
              --max-connection-per-server=16 \
              --split=16 \
              --min-split-size=1M \
              --file-allocation=none \
              --continue=true \
              --max-concurrent-downloads=5 \
              --summary-interval=30 \
              --console-log-level=notice \
              --human-readable=true \
              --download-result=full \
              || EXIT_CODE=$?
            
            END_TIME=$(date +%s)
            DURATION=$((END_TIME - START_TIME))
            
            echo ""
            echo "=========================================="
            echo "📊 下载统计"
            echo "=========================================="
            echo "⏱️ 耗时: ${DURATION} 秒"
            
            if [ "${EXIT_CODE:-0}" = "0" ]; then
              echo "✅ 下载完成!"
            elif [ "${EXIT_CODE:-0}" = "124" ]; then
              echo "⚠️ 下载超时 (${TIMEOUT_MINUTES}分钟)"
            else
              echo "❌ 下载失败 (exit code: ${EXIT_CODE:-unknown})"
            fi
            
            echo ""
            echo "📁 下载目录内容:"
            ls -lah /downloads/ 2>/dev/null || echo "  (空)"
            echo ""
            echo "📊 文件大小:"
            du -sh /downloads/* 2>/dev/null || echo "  无文件"
            echo "=========================================="
